# 答辩检查问答（供老师检查答辩用）

> 说明：本文件为课程设计答辩时老师可能检查的问题及标准答案，侧重语义分析与目标代码生成部分。

## 一、项目概述

- 问：项目做了什么，你负责哪些部分？
  - 答：实现了 SNL 编译器，包括词法（`snl.l`）、语法（`snl.y`）、语义检查（`semantic.cpp`）和 MIPS 代码生成（`codegen.cpp`）。我负责项目的整体实现、集成、测试、报告与演示准备（词法/语义/目标样例由他人负责）。

- 问：编译器的总体架构是什么？
  - 答：典型前端/后端架构：词法->语法->AST->语义检查->代码生成，模块通过 `TreeNode` AST 连接，`main.cpp` 为入口。

## 二、语义分析（重点）

- 问：语义检查主要做了哪些事情？
  - 答：建立作用域栈（符号表/类型表/初始化状态），检查未定义/重定义、把类型当变量使用、赋值与比较的类型一致性、VAR 参数左值检查、用前未初始化警告、数组常量越界与常量除零检测等。

- 问：作用域与符号表如何实现？
  - 答：使用 vector 作为栈，`enterScope()`/`leaveScope()` 管理，多级作用域从栈顶向下查找符号或类型。

- 问：如何表示与比较类型？
  - 答：用 `TypeInfo`（kind/数组范围/元素类型/记录字段映射）表示类型，`sameType()` 递归比较数组元素与记录字段结构以判断相容性。

- 问：如何处理 VAR 参数别名冲突？
  - 答：检查 VAR 参数实参为左值，记录传入变量标识；若同一变量传入多个 VAR 参数则发出警告（避免别名副作用）。

- 问：初始化跟踪如何做？
  - 答：`initScopes_` 记录变量是否已初始化；读取时报警未初始化警告，赋值/READ 会标记为已初始化。

- 问：常量折叠/求值做了什么限制？
  - 答：`tryEvalIntConst`/`tryEvalBoolConst` 能对简单常量表达式求值，用于检测恒定分支、常量越界或除零；对复杂运行时表达式不做编译期求值。

- 问：语义错误如何定位？
  - 答：AST 节点保存 `lineno`，报错信息包含行号，便于在源代码中定位问题。

- 问：遇到语义错误会怎样？
  - 答：报告错误并阻止后续代码生成（保持编译失败），错误数量会汇总返回。

## 三、目标代码生成（重点）

- 问：生成的目标是什么？调用约定如何？
  - 答：生成 MIPS32 汇编（`out.s`），过程保存 `$ra/$fp`，本地变量通过帧偏移访问，参数右到左入栈；VALUE 参数传值，VAR 参数传地址或引用。调用者负责调用后恢复栈空间。

- 问：数组与记录如何实现访问？
  - 答：记录字段预计算偏移存入 `fieldOffset`；数组访问：索引减去 `low`、乘元素字节数、加基址得到元素地址，再 `lw`/`sw` 操作。

- 问：临时寄存器如何分配？溢出如何处理？
  - 答：使用 `$t0-$t9` 池（`allocTemp`/`freeTemp`），若池耗尽会报错（这是已知局限，复杂表达式会触发）。

- 问：如何生成表达式与控制流指令？
  - 答：表达式按子树后序生成到临时寄存器，使用 MIPS 指令如 `addu/subu/mul/div/slt/seq` 等；分支用标签与条件跳转 (`beq`/`j`)。

- 问：编译器是否插入运行时越界或溢出检查？
  - 答：目前只对编译期可确定的常量越界或除零进行报错，未插入通用运行时越界或溢出检查（可作为未来优化）。

## 四、测试与验证

- 问：如何验证语义检查正确？
  - 答：使用负例测试（如 `test_sem_all12.snl`）验证能触发预期错误；错误消息与行号用于验证检测覆盖范围。

- 问：如何验证生成代码正确？
  - 答：用 `parser.exe test_codegen_ok.snl` 生成 `out.s`，用 MIPS 仿真器（Mars）运行并比对预期输出；README 中有命令与示例输出。

- 问：是否有自动化测试脚本？
  - 答：有 `run_codegen_stress_tests.ps1`，可以批量运行正例、负例与压力测试（PowerShell 下运行）。

## 五、设计决策与替代方案

- 问：为什么手写语义与代码生成，不用现成中间表示/后端？
  - 答：教学目标需要展示从前端到后端的完整实现细节，手写使设计与错误检查更透明、便于控制教学演示。

- 问：为什么选择当前的调用约定与栈布局？
  - 答：简单、清晰、便于实现 VAR 参数与局部变量管理，符合教学需求；亦便于在 MIPS 上仿真。

- 问：如果要继续改进，优先项是什么？
  - 答：添加运行时越界检查、实现寄存器分配（图着色）、引入中间表示（IR）支持多轮优化、增加更多测试覆盖。

## 六、已知限制（答辩中应主动说明）

- 动态索引越界未做运行时检查；
- 临时寄存器数量有限，深度表达式可能触及；
- 未实现复杂优化（寄存器分配/全局常量传播等）；
- 整数溢出未做捕获。

## 七、现场检查与应急（老师可能关注）

- 现场若运行失败：立即展示预生成的 `out.s`、日志和 README 中的示例输出，并说明可能原因；
- 建议带上：关键测试输入（如 `test_codegen_ok.snl`）、相应输出文本、`out.s` 的截图和 `run_codegen_stress_tests.ps1` 的输出汇总；
- 演示顺序一句话概述：`编译 -> 语义检查（报告/拒绝） -> 代码生成 -> 仿真执行（输出验证）`。

---

如果需要，我可以：

- 将此文件转换为 PDF；
- 将每个问答做成单页打印卡片（短句）供现场翻阅；
- 或把部分答案按你想强调的点调整语气和细节（例如更技术或更口语）。

